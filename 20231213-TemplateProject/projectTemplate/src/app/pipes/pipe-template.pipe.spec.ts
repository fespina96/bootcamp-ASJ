import { PipeTemplatePipe } from './pipe-template.pipe';

describe('PipeTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
